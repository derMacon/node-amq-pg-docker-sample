export class ResultWrapper {

	message: string;
	extractedElem: string;

	constructor(message: string, extractedElem: string) {
		this.message = message;
		this.extractedElem = extractedElem;
	}

}